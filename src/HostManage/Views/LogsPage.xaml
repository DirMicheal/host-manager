<Page x:Class="HostManage.Views.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="LogsPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="Magnify" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="SearchTextBox"
                             materialDesign:HintAssist.Hint="关键词搜索..."
                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                             Width="260" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <DatePicker materialDesign:HintAssist.Hint="开始日期"
                                SelectedDate="{Binding StartDate}"
                                Width="160" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="至" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker materialDesign:HintAssist.Hint="结束日期"
                                SelectedDate="{Binding EndDate}"
                                Width="160" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <TextBlock Text="级别:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox ItemsSource="{Binding LevelOptions}"
                              SelectedItem="{Binding SelectedLevel}"
                              Width="120" VerticalAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="清空过滤" Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding ClearFilterCommand}" Margin="0,0,8,0">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FilterRemove" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="清空过滤" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                    <Button Content="导出日志" Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding ExportCommand}">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Export" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="导出日志" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding FilteredLogs}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  materialDesign:DataGridAssist.CellPadding="8,4"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8,6">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="170"/>
                <DataGridTemplateColumn Header="级别" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,6,0">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="#FF2196F3"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Info">
                                                    <Setter Property="Fill" Value="#FF2196F3"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Warn">
                                                    <Setter Property="Fill" Value="#FFFF9800"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Fill" Value="#FFF44336"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Action">
                                                    <Setter Property="Fill" Value="#FF4CAF50"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="{Binding Level}" VerticalAlignment="Center" FontSize="12"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="操作" Binding="{Binding Action}" Width="140"/>
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
                <DataGridTextColumn Header="用户名" Binding="{Binding UserName}" Width="100"/>
                <DataGridTemplateColumn Header="详情" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Details}" TextTrimming="CharacterEllipsis"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
