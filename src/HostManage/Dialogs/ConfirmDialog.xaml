<Window x:Class="HostManage.Dialogs.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        d:DesignHeight="260" d:DesignWidth="480"
        Title="确认"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Width="480" SizeToContent="Height">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="24,24,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="IconBorder" Width="56" Height="56"
                    Background="#FFE3F2FD" CornerRadius="28"
                    VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,0,20,0">
                <materialDesign:PackIcon x:Name="DialogIcon" Kind="HelpCircle" Width="32" Height="32"
                                         Foreground="#FF1976D2"
                                         VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                <TextBlock x:Name="TitleText" Text="确认操作" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
                <TextBlock x:Name="MessageText" Text="确定要执行此操作吗？" TextWrapping="Wrap" FontSize="14" Foreground="#FF424242" LineHeight="22"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="24,0,24,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" x:Name="CancelButton" Content="取消" Width="110" Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="CancelButton_Click"/>
            <Button Grid.Column="2" x:Name="ConfirmButton" Content="确定" Width="110"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    Click="ConfirmButton_Click"/>
        </Grid>
    </Grid>
</Window>
