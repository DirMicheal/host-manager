<Window x:Class="HostManage.Dialogs.CompareDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:services="clr-namespace:HostManage.Services"
        mc:Ignorable="d"
        d:DesignHeight="500" d:DesignWidth="700"
        Title="环境对比"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Width="700" Height="520"
        MinWidth="600" MinHeight="400">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <materialDesign:PackIcon Kind="Compare" Width="28" Height="28"
                                     Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,10,0"/>
            <StackPanel>
                <TextBlock Text="环境与系统Hosts差异对比" FontWeight="Bold" FontSize="18"/>
                <TextBlock Text="{Binding Subtitle}" FontSize="12" Foreground="#FF757575" Margin="0,2,0,0"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,0">
                    <materialDesign:PackIcon Kind="PlusCircle" Width="16" Height="16" Foreground="#FF4CAF50" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Text="新增: " VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding AddedCount}" FontWeight="Bold" Foreground="#FF4CAF50" VerticalAlignment="Center"/>
                    <TextBlock Text=" 条" VerticalAlignment="Center" Foreground="#FF757575"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,8,0">
                    <materialDesign:PackIcon Kind="MinusCircle" Width="16" Height="16" Foreground="#FFF44336" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Text="删除: " VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding RemovedCount}" FontWeight="Bold" Foreground="#FFF44336" VerticalAlignment="Center"/>
                    <TextBlock Text=" 条" VerticalAlignment="Center" Foreground="#FF757575"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="PencilCircle" Width="16" Height="16" Foreground="#FFFF9800" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Text="修改: " VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ModifiedCount}" FontWeight="Bold" Foreground="#FFFF9800" VerticalAlignment="Center"/>
                    <TextBlock Text=" 条" VerticalAlignment="Center" Foreground="#FF757575"/>
                </StackPanel>
            </Grid>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding DiffItems}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      materialDesign:DataGridAssist.CellPadding="8,4"
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8,6">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="Added">
                                <Setter Property="Background" Value="#FFE8F5E9"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="Removed">
                                <Setter Property="Background" Value="#FFFFEBEE"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="Modified">
                                <Setter Property="Background" Value="#FFFFF8E1"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="类型" Width="100" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <materialDesign:Chip>
                                    <materialDesign:Chip.Style>
                                        <Style TargetType="materialDesign:Chip">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="Added">
                                                    <Setter Property="Content" Value="新增"/>
                                                    <Setter Property="Background" Value="#FF4CAF50"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="Removed">
                                                    <Setter Property="Content" Value="删除"/>
                                                    <Setter Property="Background" Value="#FFF44336"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="Modified">
                                                    <Setter Property="Content" Value="修改"/>
                                                    <Setter Property="Background" Value="#FFFF9800"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:Chip.Style>
                                </materialDesign:Chip>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="属性" Binding="{Binding Property}" Width="120"/>
                    <DataGridTemplateColumn Header="旧值 / 原值" Width="*" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding OldValue}" TextTrimming="CharacterEllipsis">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="Removed">
                                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="新值 / 目标值" Width="*" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding NewValue}" TextTrimming="CharacterEllipsis" FontWeight="{Binding Type, Converter={StaticResource AddedToBoldConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="关闭" Width="120"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>
