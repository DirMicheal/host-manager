<Window x:Class="HostManage.Dialogs.EnvironmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        d:DesignHeight="400" d:DesignWidth="480"
        Title="环境设置"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Width="480" Height="360">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="Layers" Width="28" Height="28"
                                     Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding DialogTitle}" FontWeight="Bold" FontSize="20" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="环境名称:" VerticalAlignment="Center" FontSize="14"/>
                <StackPanel Grid.Column="1">
                    <TextBox x:Name="NameTextBox"
                             materialDesign:HintAssist.Hint="例如: 开发环境、测试环境"
                             Text="{Binding EnvironmentName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="描述:" VerticalAlignment="Top" FontSize="14"/>
                <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="可选，环境的详细说明"
                         Text="{Binding EnvironmentDescription, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap" Height="100"
                         VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="取消" Width="100" Margin="0,0,8,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="CancelButton_Click"/>
            <Button Grid.Column="2" Content="确定" Width="100"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    Click="OkButton_Click"/>
        </Grid>
    </Grid>
</Window>
